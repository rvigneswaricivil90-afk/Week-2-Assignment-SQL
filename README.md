# Week-2-Assignment-SQL 
🛠️ Tools used : MySQL / PostgreSQL 
⚙️ Steps to Implement:
      • Created database and table 
      • Inserted sample data 
• Written queries:  
       SELECT * FROM sales; 
• Used aggregation: 
       SELECT SUM(sales) FROM table; 
• Used GROUP BY: 
       SELECT product, SUM(sales) FROM table GROUP BY product
• Used JOIN (if multiple tables)
       Used Inner join to connect two tables with foreign keys and primary keys
